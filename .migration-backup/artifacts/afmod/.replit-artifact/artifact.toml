kind = "web"
previewPath = "/"
title = "ArabFirstWeb"
version = "1.0.0"
id = "artifacts/afmod"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20751

[services.development]
run = "pnpm --filter @workspace/afmod run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/afmod", "run", "build" ]
publicDir = "artifacts/afmod/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20751"
BASE_PATH = "/"

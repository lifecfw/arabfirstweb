kind = "web"
previewPath = "/"
title = "Ministry of Logistics — AFMOD"
version = "1.0.0"
id = "artifacts/afmod"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 3000

[services.development]
run = "pnpm --filter @workspace/afmod run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/afmod", "run", "build" ]
publicDir = "artifacts/afmod/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "3000"
BASE_PATH = "/"
